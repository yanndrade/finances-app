# Schemas

Use this directory for shared validation schemas and serialized payload definitions.

When a schema change affects frontend/backend compatibility, update `../version.txt` in the same change.
