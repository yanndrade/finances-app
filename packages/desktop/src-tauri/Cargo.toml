[package]
name = "finances-desktop"
version = "0.1.0"
description = "Desktop shell for the finances app"
authors = ["yannb"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0.6", features = [] }

[dependencies]
tauri = { version = "2.8.5", features = ["tray-icon"] }
tauri-plugin-autostart = "2"

[features]
default = []
