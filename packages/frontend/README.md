# Frontend

Frontend package root for the React application shared by desktop and mobile LAN access.

- `src/pages`: route-level screens
- `src/components`: reusable presentation components
- `src/features`: feature-scoped UI and state
- `src/lib`: shared frontend utilities and adapters
- `public/`: static assets

Framework setup files are intentionally deferred to the frontend foundation issues.
