# Packages

This directory contains the explicit package boundaries of the monorepo.

- `shared`: cross-package schemas and types
- `backend`: application services and persistence
- `frontend`: React UI code
- `desktop`: Tauri shell and desktop-specific integration
