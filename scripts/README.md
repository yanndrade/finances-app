# Scripts

Utility scripts for local development, verification, builds, and repository maintenance.

The current script `check-monorepo-structure.ps1` validates the base scaffold introduced by issue `#8`.
